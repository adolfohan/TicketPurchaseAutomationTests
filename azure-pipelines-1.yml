trigger:
- master

pool:
  vmImage: 'windows-latest'

variables:
  solution: '**/*.sln'
  buildPlatform: 'Any CPU'
  buildConfiguration: 'Release'

steps:
- task: NuGetToolInstaller@1

- task: NuGetCommand@2
  inputs:
    restoreSolution: '$(solution)'

- task: VSBuild@1
  inputs:
    solution: '$(solution)'

- task: VSTest@2
  displayName: 'Ticket Shop Tests'
  inputs:
    testAssemblyVer2: '**\TicketShopTestCases.csproj'
    searchFolder: 'TicketShopTestCases/Tests/TicketPurchaseBaseTest.cs'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'